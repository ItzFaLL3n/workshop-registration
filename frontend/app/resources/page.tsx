import type { Metadata } from "next";
import { Component } from "@/components/ui/dev-tool-landing-page";

export const metadata: Metadata = {
  title: "Setup Guide & Resources — LLM Agents Workshop",
  description:
    "Everything to prep for the workshop: install Python 3.10+, create a virtual environment, download the 14-demo pack, and install dependencies. Runs fully offline.",
};

export default function ResourcesPage() {
  return <Component />;
}
