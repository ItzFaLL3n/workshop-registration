import type { Metadata } from "next";
import { Component } from "@/components/ui/dev-tool-landing-page";

export const metadata: Metadata = {
  title: "Installation Guide — LLM Agents Workshop",
  description:
    "Step-by-step setup guide for the LLM Agents workshop. Install Python, Ollama, LangChain, and LlamaIndex before Day 1.",
};

export default function InstallPage() {
  return <Component />;
}
