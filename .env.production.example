# Copy to .env.production.local? No — docker-compose reads a file literally
# named `.env` in this directory by default. Copy this file to `.env` on the
# VM (not into git) and fill in real values before running `docker compose up -d`.
#
# Frontend is hosted separately on Netlify (not in this compose stack) — this
# VM only runs postgres + backend + Caddy, serving the API directly.

# ── Domain (used by Caddy for automatic HTTPS) ──
# The hostname the API is reachable at — e.g. your VM's free Azure DNS label
# (yourlabel.<region>.cloudapp.azure.com) or a custom domain/subdomain.
# Must resolve to this VM's public IP before starting the stack, or Let's
# Encrypt cert issuance will fail.
DOMAIN=yourlabel.centralindia.cloudapp.azure.com

# ── Postgres (container-internal only, never exposed publicly) ──
POSTGRES_USER=workshop
POSTGRES_PASSWORD=change-this-to-something-long-and-random
POSTGRES_DB=workshop_registration
